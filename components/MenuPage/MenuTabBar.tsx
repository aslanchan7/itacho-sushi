import { MenuType } from '@/app/menu/page';
import styles from './MenuTabBar.module.css'

export default function MenuTabBar({ menuType } : { menuType: MenuType }) {
    return (
        <section>
            
        </section>
    )
}